{{- if .Values.notifications.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "jobrecon.fullname" . }}-notifications
  labels:
    {{- include "jobrecon.serviceLabels" (dict "Chart" .Chart "Release" .Release "component" "notifications") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.notifications.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jobrecon.serviceSelectorLabels" (dict "Chart" .Chart "Release" .Release "component" "notifications") | nindent 4 }}
{{- end }}
